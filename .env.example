# Exemple de fichier de configuration — copier en .env et adapter
# cp .env.example .env

# Environnement (development | production)
ENV=production

# Clé secrète pour les sessions (si nécessaire)
# SECRET_KEY=changeme

# Clé API OpenAI (pour le chatbot, optionnel en V1)
# OPENAI_API_KEY=sk-...
